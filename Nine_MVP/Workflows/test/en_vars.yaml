VAR_METADATA_COLUMNS:  "asset_id" # Comma separated string
VAR_PAGE_CONTENT_COLUMNS: "content,test_metadata"  # Comma separated string
VAR_SOURCE_QUERY: "SELECT id as asset_id,media_type,content,test_metadata, path  FROM `nine-quality-test.Nine_Quality_Test.test_long_artcles` ;"        
VAR_CHUNK_SIZE: 500
VAR_CHUNK_OVERLAP:  10
VAR_MAX_PROMPT_COUNT_LIMIT: 2 #30000 #Maximum number of requests per batch; currently, the API has a limit of 30,000 prompts

